service: rag-chatbot

provider:
  name: aws
  runtime: nodejs22.x
  region: us-east-1

plugins:
  - serverless-offline

functions:
  chatFunction:
    handler: handler.chatHandler
    events:
      - http:
          path: chat
          method: post
